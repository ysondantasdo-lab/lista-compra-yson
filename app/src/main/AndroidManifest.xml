<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://google.com"
    xmlns:tools="http://google.com">
    
    <!-- Permissão de Internet necessária para sincronizar a lista com o Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaCompras"
        tools:node="merge">
        
        <activity
            android:name="br.com.yson.listacompras.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ListaCompras">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    </application>
</manifest>
